package ejbholidaybookingapp;

import javax.ejb.Remote;

@Remote
public interface EmployeeRoleAppBeanRemote {
	EmployeeRoleDTO getHeadRole(String upperName);
	EmployeeRoleDTO getDeputyHeadRole(String upperName);
}
